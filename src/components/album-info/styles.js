import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
  summary: {
    fontSize: 14,
    textAlign: 'justify',
    marginTop: 10,
    marginBottom: 20
  },
  music: {
    marginRight: 10,
    marginBottom: 5,
    maxWidth: '100%'
  }
}));
