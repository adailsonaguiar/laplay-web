import { createMuiTheme } from '@material-ui/core/styles';

export const outerTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#F2BB00',
      contrastText: '#fff',
      dark: '#EBB500'
    },
    secondary: {
      main: '#18171D',
      contrastText: '#fff',
      dark: '#18171D'
    }
  }
});
